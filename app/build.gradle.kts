android {
    namespace = "com.vidkingtv.app"
    compileSdk = 34 // Đảm bảo có dấu "="

    defaultConfig {
        applicationId = "com.vidkingtv.app" // Đảm bảo có dấu "=" và dùng ngoặc kép ""
        // Giữ nguyên các cấu hình khác (minSdk, targetSdk, versionCode...)
    }
    
    // Giữ nguyên các cấu hình buildTypes, compileOptions...
}
